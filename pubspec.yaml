name: rentcontrol
description: "A new Flutter project."

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  mockito: ^5.4.0
  http: ^1.4.0
  auth_package:
    git:
      url: https://github.com/DavyFelix/auth_package.git
      ref: main
  firebase_core: ^2.25.0
  firebase_auth: ^4.17.0
  cloud_firestore: ^4.8.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

 
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/images/predios.jpg
    - assets/images/predios_inicio.png


 